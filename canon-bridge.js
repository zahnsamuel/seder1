const learnerId=Seder.currentLearnerId(),id=new URLSearchParams(location.search).get('bridge'),$=s=>document.querySelector(s);fetch('/api/curriculum/canon-bridges').then(r=>r.json()).then(data=>{const bridge=data.bridges.find(x=>x.id===id)||data.bridges[0],ready=localStorage.getItem(`seder-capstone-${bridge.from}-${learnerId}`);if(!ready){$('#title').textContent='This connection opens after its capstone';$('#intro').textContent='Complete the preceding course and write its independent capstone before carrying that source move into the next course.';$('#sources').innerHTML=`<article class="source"><div class="citation">PRECEDING COURSE</div><p><a href="canon-capstone.html?course=${bridge.from}">Return to the capstone →</a></p></article>`;return}$('#title').textContent=bridge.title;$('#sources').innerHTML=`<article class="source"><div class="citation">FROM · ${bridge.fromSource}</div><p class="line-note">${bridge.move}</p></article><article class="source"><div class="citation">TO · ${bridge.toSource}</div><p class="line-note">The next course develops this new setting.</p></article>`;$('#prompt').textContent=bridge.prompt;const choices=[bridge.move,'Treat both sources as saying exactly the same thing.','Ignore the setting of the later source.'].sort(()=>Math.random()-.5);$('#choices').innerHTML=choices.map((choice,index)=>`<button class="choice" data-choice="${index}">${choice}</button>`).join('');document.querySelectorAll('[data-choice]').forEach(button=>button.onclick=async()=>{const correct=button.textContent===bridge.move;document.querySelectorAll('[data-choice]').forEach(item=>item.disabled=true);$('#feedback').textContent=correct?'Bridge demonstrated. You preserved continuity and difference between the sources.':'Return to both source labels: the bridge depends on preserving what each text is doing.';if(correct){localStorage.setItem(`seder-bridge-${bridge.id}-${learnerId}`,'complete');$('#next').innerHTML=`<a class="source-link" href="canon-course.html?course=${bridge.to}">Continue into the next course →</a>`}try{await Seder.api(`/api/learners/${learnerId}/events`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({type:'answer_submitted',skillId:bridge.skill,competency:'sourceReasoning',sourceContext:`Canon bridge: ${bridge.title}`,correct})})}catch(e){console.warn(e)}})}).catch(()=>{$('#title').textContent='Bridge temporarily unavailable'})
