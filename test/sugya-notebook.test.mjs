import test from 'node:test'; import assert from 'node:assert/strict'; import { readFile } from 'node:fs/promises';
test('sugya notebook records the components of learner-owned Gemara reading', async () => { const [html, js, mastery] = await Promise.all(['sugya-notebook.html', 'sugya-notebook.js', 'tractate-mastery.js'].map((file) => readFile(new URL(`../${file}`, import.meta.url), 'utf8'))); for (const id of ['case', 'question', 'response', 'words', 'explanation']) assert.match(html, new RegExp(`id="${id}"`)); assert.match(js, /sugya_map_saved/); assert.match(mastery, /Sugya notebook/); });
