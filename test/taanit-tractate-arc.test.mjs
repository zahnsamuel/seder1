import test from 'node:test';import assert from 'node:assert/strict';import{readFile}from'node:fs/promises';test('Taanit offers a source-grounded timing-dispute arc and visible workspace',async()=>{const[a,p,w,h,d,e]=await Promise.all(['taanit-arc.js','taanit-arc.html','taanit-daf-workbench.js','taanit-daf-workbench.html','data/gemara-tractates.json','course-engine.js'].map(f=>readFile(f,'utf8')));for(const x of['taanit-timing-question','taanit-timing-reason','taanit-mention-request-distinction','taanit-placement-question','taanit-independent-map'])assert.match(a,new RegExp(x));assert.match(a,/Taanit 2a/);assert.match(a,/const taanitSteps=window\.SederCourse\.steps/);assert.match(p,/not medical, weather, or practical halakhic guidance/i);assert.match(w,/Placement question/);assert.match(h,/Taanit 2a/);assert.match(e,/'taanit-tractate-arc':\{tractate:'taanit'/);const t=JSON.parse(d).tractates.find(x=>x.title==='Taanit');assert.equal(t.stage,'tractate-arc');assert.equal(t.arcUrl,'taanit-arc.html')});
