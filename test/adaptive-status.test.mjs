import test from 'node:test'; import assert from 'node:assert/strict'; import { readFile } from 'node:fs/promises';
test('daily path surfaces mastery status and links to an adaptive Gemara skill map', async () => { const [html, status, map] = await Promise.all(['daily-router.html', 'adaptive-status.js', 'adaptive-gemara-map.js'].map((file) => readFile(new URL(`../${file}`, import.meta.url), 'utf8'))); assert.match(html, /mastery-status/); assert.match(status, /Ready to transfer/); assert.match(status, /adaptive-gemara-map\.html/); assert.match(map, /Read an unfamiliar sugya/); });
