import test from 'node:test';import assert from 'node:assert/strict';import{readFile}from'node:fs/promises';test('shared mobile study styles protect touch targets Hebrew reading and journey layout',async()=>{const s=await readFile(new URL('../seder-auth.js',import.meta.url),'utf8');for(const x of ['applyMobileStudyStyles','min-height:44px','line-hebrew','grid-template-columns:repeat(2','reader textarea'])assert.ok(s.includes(x));});
