import test from 'node:test';import assert from 'node:assert/strict';import{readFile}from'node:fs/promises';test('review calendar makes due and upcoming source retrieval visible',async()=>{const [h,j]=await Promise.all(['review-calendar.html','review-calendar.js'].map(f=>readFile(new URL(`../${f}`,import.meta.url),'utf8')));assert.match(h,/REVIEW CALENDAR/);assert.match(j,/upcoming/);assert.match(j,/review\.html/);});
