import test from 'node:test';import assert from 'node:assert/strict';import {readFile} from 'node:fs/promises';test('second foundation term opens through Ketubot with shuffled source reasoning',async()=>{const [h,j]=await Promise.all(['second-foundation-term.html','second-foundation-term.js'].map(x=>readFile(new URL(`../${x}`,import.meta.url),'utf8')));assert.match(h,/KETUBOT 1:1/);assert.match(h,/STUDY BOUNDARY/);assert.match(j,/Math\.random/);assert.match(j,/ketubot-arc\.html/)});
