import test from 'node:test';import assert from 'node:assert/strict';import {readFile} from 'node:fs/promises';test('Return Cycle is an earned three-move checkpoint',async()=>{const[p,j]=await Promise.all([readFile(new URL('../return-cycle-two.html',import.meta.url),'utf8'),readFile(new URL('../return-cycle-two.js',import.meta.url),'utf8')]);assert.match(p,/return-cycle-two\.js/);for(const x of['return-eruvin','return-pesachim','return-widerworld',"stageId:'return-cycle-two'"])assert.match(j,new RegExp(x.replace(/[.?]/g,'\\$&')))});
