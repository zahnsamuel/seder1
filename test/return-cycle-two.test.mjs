import test from 'node:test';import assert from 'node:assert/strict';import {readFile} from 'node:fs/promises';test('return cycle connects Eruvin and Pesachim to a responsible wider-world comparison',async()=>{const j=await readFile(new URL('../return-cycle-two.js',import.meta.url),'utf8');for(const x of['Eruvin 2a','Pesachim 2a','Wider World','return-widerworld'])assert.match(j,new RegExp(x.replace(/[.?]/g,'\\$&')))});
