const learnerId=Seder.currentLearnerId(),lines=[{hebrew:'כֵּיצַד הָעֵדִים נַעֲשִׂים זוֹמְמִין?',translation:'How do witnesses become conspiring witnesses?',role:'Case question',clue:'The Mishnah begins by defining how a category is established.'},{hebrew:'וַעֲשִׂיתֶם לוֹ כַּאֲשֶׁר זָמַם לַעֲשׂוֹת לְאָחִיו',translation:'You shall do to him as he schemed to do to his brother.',role:'Torah source',clue:'The verse supplies the source of the reciprocal consequence.'},{hebrew:'כַּאֲשֶׁר זָמַם וְלֹא כַּאֲשֶׁר עָשָׂה',translation:'As he schemed — and not as he did.',role:'Close reading',clue:'The Gemara reads one word to draw a boundary.'},{hebrew:'הֲזָמָה · לֹא הַכְחָשָׁה',translation:'Exposure by alibi · not mere contradiction.',role:'Witness distinction',clue:'The source separates discrediting witnesses from competing accounts.'},{hebrew:'עֵדוּת · זָמַם · תּוֹצָאָה · גְּבוּל',translation:'Testimony · scheme · consequence · boundary.',role:'Condition map',clue:'The completed map keeps category, source, consequence, and limit together.'}];const $=s=>document.querySelector(s);let focused=null,showTranslation=false;const mapped=new Map();function renderMap(){const complete=mapped.size===lines.length;$('#argumentMap').innerHTML=lines.map((x,i)=>`<li class="${mapped.has(i)?'mapped':''}"><b>${i+1}</b><span>${mapped.get(i)||'Unclassified'}</span></li>`).join('');$('#mapProgress').textContent=complete?'Source map earned: you named the category through its source and limit.':`${mapped.size} of ${lines.length} source moves classified.`;$('#nextMove').hidden=!complete;if(complete)$('#nextMove').innerHTML='<strong>Makkot source map complete</strong><a href="makkot-arc.html">Revisit the Makkot source trail →</a>'}function render(){$('#lines').innerHTML=lines.map((x,i)=>`<button class="daf-line ${focused===i?'focused':''} ${mapped.has(i)?'mapped':''}" data-line="${i}"><b>${i+1}</b><span lang="he" dir="rtl">${x.hebrew}</span>${showTranslation?`<small>${x.translation}</small>`:''}</button>`).join('');document.querySelectorAll('.daf-line').forEach(b=>b.onclick=()=>{focused=Number(b.dataset.line);$('#role').value=mapped.get(focused)||'';$('#mark').disabled=false;const x=lines[focused];$('#lineTitle').textContent=`Line ${focused+1}: read its job`;$('#lineTranslation').textContent=x.translation;$('#clues').textContent=x.clue;render()})}$('#toggleTranslation').onclick=()=>{showTranslation=!showTranslation;$('#toggleTranslation').textContent=showTranslation?'Hide translations':'Show translations';render()};$('#mark').onclick=async()=>{if(focused===null||!$('#role').value)return;const x=lines[focused],correct=$('#role').value===x.role;$('#feedback').textContent=correct?'Good reading.':'Try again: use the clue.';if(correct){mapped.set(focused,x.role);render();renderMap()}const r=await Seder.api(`/api/learners/${learnerId}/events`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({type:'source_annotation',skillId:'makkot-independent-map',competency:'argument',sourceContext:`Makkot workspace line ${focused+1}`,correct})});if(r.ok)$('#xp').textContent=`${(await r.json()).xp} XP`};Seder.api(`/api/learners/${learnerId}`).then(r=>r.ok?r.json():null).then(l=>$('#xp').textContent=`${l?.xp||0} XP`);render();renderMap();
