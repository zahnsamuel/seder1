const learnerId=Seder.currentLearnerId(),choices=['What practical or institutional concern the schedule is designed to serve.','Which weekday sounds most meaningful today.','Whether a later custom can replace the Mishnah’s own question.'],shuffled=choices.map((text,index)=>({text,index})).sort(()=>Math.random()-.5);document.querySelector('#answers').innerHTML=shuffled.map(({text,index})=>`<button class="choice" data-choice="${index}">${text}</button>`).join('');document.querySelectorAll('[data-choice]').forEach(button=>button.onclick=async()=>{const correct=Number(button.dataset.choice)===0;document.querySelectorAll('[data-choice]').forEach(x=>x.disabled=true);button.classList.add(correct?'good':'bad');await Seder.api(`/api/learners/${learnerId}/events`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({type:'answer_submitted',skillId:'second-foundation-ketubot-reason',competency:'sourceReasoning',sourceContext:'Mishnah Ketubot 1:1',correct})}).catch(()=>{});document.querySelector('#feedback').innerHTML=correct?'<span class="feedback">+15 XP. A schedule is often an argument about what the institution needs to protect.</span><p><a class="continue" href="ketubot-arc.html">Begin Ketubot Block 9 →</a></p>':'<span class="feedback">Not yet. Return to the wording and ask what kind of institutional problem a fixed date might answer.</span><p><button class="continue" onclick="location.reload()">Try again →</button></p>';if(correct)await Seder.api(`/api/learners/${learnerId}/events`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({type:'stage_mastered',stageId:'second-foundation-entry'})}).catch(()=>{})});
