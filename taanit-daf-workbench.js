const learnerId=Seder.currentLearnerId(),lines=[{hebrew:'מאימתי מזכירין גבורות גשמים?',translation:'From when do we mention the might of the rains?',role:'Timing question',clue:'The source opens by asking when a practice begins.'},{hebrew:'רבי אליעזר אומר מיום טוב הראשון של חג',translation:'Rabbi Eliezer says: From the first festival day.',role:'First position',clue:'Attach this speaker to an early starting point.'},{hebrew:'רבי יהושע אומר מיום טוב האחרון של חג',translation:'Rabbi Yehoshua says: From the last festival day.',role:'Second position',clue:'This is a second answer to the same timing question.'},{hebrew:'הואיל ואין הגשמים אלא סימן קללה בחג',translation:'Since rain during the festival is only a sign of a curse.',role:'Stated reason',clue:'A concern about the festival is offered as an argument about timing.'},{hebrew:'אף אני לא אמרתי לשאול אלא להזכיר',translation:'I did not say to request, but only to mention.',role:'Distinction',clue:'The reply separates two related practices.'},{hebrew:'וליתני התם? מאי שנא דשבקיה עד הכא?',translation:'Let it be taught there; what is different that it was left until here?',role:'Placement question',clue:'The Gemara asks why this topic is located in this tractate.'}];const $=s=>document.querySelector(s);let focused=null,showTranslation=false;const mapped=new Map();function renderMap(){const complete=mapped.size===lines.length;$('#argumentMap').innerHTML=lines.map((x,i)=>`<li class="${mapped.has(i)?'mapped':''}"><b>${i+1}</b><span>${mapped.get(i)||'Unclassified'}</span></li>`).join('');$('#mapProgress').textContent=complete?'Source map earned: you named the argument from question through placement.':`${mapped.size} of ${lines.length} source moves classified.`;$('#nextMove').hidden=!complete;if(complete)$('#nextMove').innerHTML='<strong>Taanit source map complete</strong><a href="taanit-arc.html">Revisit the Taanit source trail →</a>'}function render(){$('#lines').innerHTML=lines.map((x,i)=>`<button class="daf-line ${focused===i?'focused':''} ${mapped.has(i)?'mapped':''}" data-line="${i}"><b>${i+1}</b><span lang="he" dir="rtl">${x.hebrew}</span>${showTranslation?`<small>${x.translation}</small>`:''}</button>`).join('');document.querySelectorAll('.daf-line').forEach(b=>b.onclick=()=>{focused=Number(b.dataset.line);$('#role').value=mapped.get(focused)||'';$('#mark').disabled=false;const x=lines[focused];$('#lineTitle').textContent=`Line ${focused+1}: read its job`;$('#lineTranslation').textContent=x.translation;$('#clues').textContent=x.clue;render()})}$('#toggleTranslation').onclick=()=>{showTranslation=!showTranslation;$('#toggleTranslation').textContent=showTranslation?'Hide translations':'Show translations';render()};$('#mark').onclick=async()=>{if(focused===null||!$('#role').value)return;const x=lines[focused],correct=$('#role').value===x.role;$('#feedback').textContent=correct?'Good reading.':'Try again: use the clue.';if(correct){mapped.set(focused,x.role);render();renderMap()}const r=await Seder.api(`/api/learners/${learnerId}/events`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({type:'source_annotation',skillId:'taanit-independent-map',competency:'argument',sourceContext:`Taanit 2a workspace line ${focused+1}`,correct})});if(r.ok)$('#xp').textContent=`${(await r.json()).xp} XP`};Seder.api(`/api/learners/${learnerId}`).then(r=>r.ok?r.json():null).then(l=>$('#xp').textContent=`${l?.xp||0} XP`);render();renderMap();
