const learnerId = Seder.currentLearnerId(); const $ = (selector) => document.querySelector(selector);
Promise.all([Seder.api(`/api/learners/${learnerId}`).then((response) => response.json()), fetch('/api/curriculum/repair-router').then((response) => response.json())]).then(([learner, router]) => { const struggles = learner.struggles || {}; const category = router.categories.map((item) => ({ ...item, misses: item.skills.reduce((total, skill) => total + (struggles[skill] || 0), 0) })).sort((a, b) => b.misses - a.misses)[0]; const repair = category?.misses ? category : { title: 'Build a new source move', url: 'daily-router.html', reason: 'No repeated failure pattern is waiting. New learning and timely retrieval are the right next tests.' }; $('#reason').textContent = repair.reason; $('#loop').innerHTML = `<article class="course-card"><small>1. DIAGNOSE</small><h2>${repair.title}</h2><p>${repair.misses || 0} repeated miss${repair.misses === 1 ? '' : 'es'} point to this specific reading move.</p></article><article class="course-card"><small>2. REBUILD</small><h2>Practice the same move</h2><p>Return to a short source where the signal is visible and get immediate feedback.</p><a href="${repair.url}">Open targeted repair &rarr;</a></article><article class="course-card"><small>3. PROVE TRANSFER</small><h2>Read an unfamiliar source</h2><p>Mastery means recognizing the move when the topic and wording change.</p><a href="independent-reading.html">Open unseen-source check &rarr;</a></article><article class="course-card"><small>4. SCHEDULE</small><h2>Retrieve it later</h2><p>A successful transfer earns a longer interval; uncertainty returns sooner for another short review.</p><a href="review.html">See scheduled retrieval &rarr;</a></article>`; });
