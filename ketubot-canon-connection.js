const learnerId=Seder.currentLearnerId(),answers=['Both sources show that legal or institutional structure can attend to a household’s human dignity, while each does that work in its own context.','The verse makes Ketubot’s historical wedding schedule automatically binding in every setting.','Ketubot’s court-access reasoning and the verse’s household concern are identical claims.'].map((text,index)=>({text,index})).sort(()=>Math.random()-.5);document.querySelector('#answers').innerHTML=answers.map(({text,index})=>`<button class="choice" data-a="${index}">${text}</button>`).join('');document.querySelectorAll('[data-a]').forEach(b=>b.onclick=async()=>{const correct=Number(b.dataset.a)===0;document.querySelectorAll('[data-a]').forEach(x=>x.disabled=true);b.classList.add(correct?'good':'bad');await Seder.api(`/api/learners/${learnerId}/events`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({type:'answer_submitted',skillId:'ketubot-canon-dignity',competency:'sourceReasoning',sourceContext:'Deuteronomy 24:5 · Ketubot 2a',correct})}).catch(()=>{});document.querySelector('#feedback').innerHTML=correct?'<span class="feedback">+15 XP. You connected a shared concern without treating two distinct sources as interchangeable.</span><a class="continue" href="ketubot-transfer.html">Practice the transfer →</a>':'<button class="continue" onclick="location.reload()">Try again →</button>';});
