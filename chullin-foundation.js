const learnerId=Seder.currentLearnerId(),a=['State the broad rule, identify who or what the exception limits, then ask why that limit matters.','Treat the exception as proof that the rule has no meaning.','Skip the rule and memorize only the exception.'].map((text,index)=>({text,index})).sort(()=>Math.random()-.5);document.querySelector('#answers').innerHTML=a.map(({text,index})=>`<button class="choice" data-a="${index}">${text}</button>`).join('');document.querySelectorAll('[data-a]').forEach(b=>b.onclick=async()=>{const correct=+b.dataset.a===0;document.querySelectorAll('[data-a]').forEach(x=>x.disabled=true);b.classList.add(correct?'good':'bad');await Seder.api(`/api/learners/${learnerId}/events`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({type:'answer_submitted',skillId:'chullin-rule-exception-entry',competency:'sourceReasoning',sourceContext:'Mishnah Chullin 1:1',correct})}).catch(()=>{});document.querySelector('#feedback').innerHTML=correct?'<span class="feedback">+15 XP. An exception can clarify a rule’s scope instead of erasing it.</span><a class="continue" href="chullin-arc.html">Begin the Chullin source trail →</a>':'<button class="continue" onclick="location.reload()">Try again →</button>';});
