const learnerId=Seder.currentLearnerId(),a=['It offers an institutional reason for the fixed wedding schedule.','It introduces an unrelated new topic.','It repeats the Mishnah without answering the question.'].map((text,index)=>({text,index})).sort(()=>Math.random()-.5);document.querySelector('#answers').innerHTML=a.map(({text,index})=>`<button class="choice" data-a="${index}">${text}</button>`).join('');document.querySelectorAll('[data-a]').forEach(b=>b.onclick=async()=>{const correct=+b.dataset.a===0;document.querySelectorAll('[data-a]').forEach(x=>x.disabled=true);b.classList.add(correct?'good':'bad');await Seder.api(`/api/learners/${learnerId}/events`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({type:'answer_submitted',skillId:'ketubot-daf-retrieval',competency:'sourceReasoning',sourceContext:'Ketubot 2a Daf retrieval',correct})}).catch(()=>{});document.querySelector('#feedback').innerHTML=correct?'<span class="feedback">+15 XP. You recovered the argument: rule, question, institutional reason.</span><a class="continue" href="ketubot-transfer.html">Continue to unseen transfer →</a>':'<button class="continue" onclick="location.reload()">Try again →</button>';});
