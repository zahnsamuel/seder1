const learnerId=Seder.currentLearnerId(),key=`seder-berakhot-lab-${learnerId}`,mastered=new Set(JSON.parse(localStorage.getItem(key)||'[]'));let lab,index=0,selected,answered=false;const $=s=>document.querySelector(s),norm=s=>String(s||'').toLowerCase().trim().replace(/[^a-z ]/g,'').replace(/\s+/g,' ');function render(){const e=lab.exercises[index],done=mastered.has(e.id);$('#progress').textContent=`${mastered.size} / ${lab.exercises.length} moves demonstrated`;$('#exercise-nav').innerHTML=lab.exercises.map((x,i)=>`<button class="lab-button ${i===index?'active':''}" data-i="${i}">${x.mode}<small>${mastered.has(x.id)?'Demonstrated':x.title}</small></button>`).join('');document.querySelectorAll('[data-i]').forEach(b=>b.onclick=()=>{index=+b.dataset.i;answered=false;render()});let response=e.acceptable?`<div class="seder-typed"><input id="typed" placeholder="Type your answer…"><button id="check">Check</button></div>`:`<div class="choices">${e.choices.map((x,i)=>`<button class="choice" data-choice="${i}">${x}</button>`).join('')}</div><button id="check" class="continue" disabled>Check my reading</button>`;$('#lesson').innerHTML=`<p class="lesson-label">${e.mode}</p><h2>${e.title}</h2><article class="source"><p class="hebrew" lang="he" dir="rtl">${e.source}</p><p class="translation">${e.translation}</p></article><p class="prompt">${e.prompt}</p>${response}<div id="feedback"></div>`;if(e.acceptable){$('#check').onclick=()=>answer(e,e.acceptable.some(a=>norm(a)===norm($('#typed').value)))}else{document.querySelectorAll('[data-choice]').forEach(b=>b.onclick=()=>{selected=+b.dataset.choice;document.querySelectorAll('[data-choice]').forEach(x=>x.classList.toggle('selected',+x.dataset.choice===selected));$('#check').disabled=false});$('#check').onclick=()=>answer(e,selected===e.correct)}}async function answer(e,correct){if(answered)return;answered=true;document.querySelectorAll('button').forEach(b=>{if(b.closest('#lesson'))b.disabled=true});$('#feedback').innerHTML=`<p class="feedback">${correct?'Demonstrated. ':'Repair this move, then return. '}${e.explanation}</p>${correct?'':`<p><a href="pilot-repair.html?skill=${e.skillId}">Open a focused repair →</a></p>`}`;if(correct){mastered.add(e.id);localStorage.setItem(key,JSON.stringify([...mastered]))}try{await Seder.api(`/api/learners/${learnerId}/events`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({type:'answer_submitted',skillId:e.skillId,competency:'argument',sourceContext:'Berakhot opening lab',correct})})}catch(x){console.warn(x)}}fetch('/api/curriculum/berakhot-practice-lab').then(r=>r.json()).then(x=>{lab=x;$('#title').textContent=x.title;$('#description').textContent=x.description;render()})
